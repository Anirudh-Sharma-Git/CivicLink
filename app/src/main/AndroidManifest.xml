<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CivicLink2O"
        tools:targetApi="31">

        <!-- Activity 1: The Splash Screen. This is now the LAUNCHER. -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"> <!-- Added theme for a true fullscreen splash -->
            <!-- This intent-filter tells Android this is the starting activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity 2: The Language Selection Screen is now a regular activity. -->
        <activity
            android:name=".LanguageSelectionActivity"
            android:exported="false" /> <!-- Must be false for non-launcher activities -->

        <!-- Activity 3: The Main Screen. -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

    </application>

</manifest>

