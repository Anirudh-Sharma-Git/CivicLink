<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/leaderboard_coming_soon_bg" />
    <stroke
        android:width="2dp"
        android:color="@color/logo_background"
        android:dashWidth="10dp"
        android:dashGap="8dp" />
    <corners android:radius="16dp" />
</shape>

