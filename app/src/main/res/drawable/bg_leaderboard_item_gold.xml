<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/leaderboard_gold_bg" />
    <stroke
        android:width="2dp"
        android:color="@color/leaderboard_gold_stroke"
        android:dashWidth="10dp"
        android:dashGap="8dp" />
    <corners android:radius="16dp" />
</shape>

