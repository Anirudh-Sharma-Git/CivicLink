<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- A light grey fill color -->
    <solid android:color="@color/upvote_bg" />
    <!-- Rounded corners to match the other input fields -->
    <corners android:radius="8dp" />
    <!-- A light grey border -->
    <stroke android:width="1dp" android:color="@color/input_field_stroke" />
</shape>